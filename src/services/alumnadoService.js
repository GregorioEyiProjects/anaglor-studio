export const DEMO_CONFIRMED_STUDENT_COUNT = 32;

export const getConfirmedStudentCount = async () => {
  // Integration point: replace this demo source with the confirmed-enrollment count.
  return DEMO_CONFIRMED_STUDENT_COUNT;
};
